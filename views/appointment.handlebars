<div>
   <h2>Appointments</h2>
 <div class="card bg-light mb-3" style="max-width: 18rem;">
         {{#each appointment}}
  <div class="card-header">Appointment<button id={{appointment.id}} class="btn-trash">🗑</button></div>
  <div class="card-body">
    <h5 class="card-title"></h5>
    <p class="appointment-card">
      <ul class="list-group list-group-flush">
         <li class="list-group-item">Appointment Date: {{date}}</li>
         <li class="list-group-item">Appointment Time: {{time}}</li>
      </ul>
   </p>
  </div>
         {{/each}}
</div>
<form class="appointment-form">
    <input type="date" class="date" name="date"
       value="2018-07-22"
       min="2018-01-01">
    <input type="time" id="appt" name="time">

    <button
      type="submit"
      class="create btn btn-outline-success btn-sm"
    >Create</button>
  </form>
</div>

<script src="/js/appointment.js"></script>
