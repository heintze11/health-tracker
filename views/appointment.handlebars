<div>
   <h2>Appointments</h2>
 <div class="card bg-light mb-3" style="max-width: 18rem;">
         {{#each appointment}}
  <div class="card-header">Appointment <button class="btn-trash">🗑</button></div>
  <div class="card-body">
    <h5 class="card-title"></h5>
    <p class="card-text">
      <ul class="list-group list-group-flush">
         <li class="list-group-item">Appointment Date: {{date}}</li>
         <li class="list-group-item">Appointment Time: {{time}}</li>
      </ul>
   </p>
  </div>
         {{/each}}
</div>
<form class="form">
    <input type="text" name="date" placeholder="Appointment Date" />
    <input type="text" name="time" placeholder="Appointment Time" />

    <button
      type="submit"
      class="create btn btn-outline-success btn-sm"
    >Create</button>
  </form>
</div>

<script src="/js/appointment.js"></script>
