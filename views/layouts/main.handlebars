<html lang="en">

  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Health Tracker</title>
    <link rel="stylesheet" href="/css/jass.css" />
    <link rel="stylesheet" href="/css/style.css" />
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=Montserrat:wght@400;500;600&display=swap"
      rel="stylesheet"
    />
    <link href="/css/main.css" rel="stylesheet" />
    <script src="/js/main.js"></script>

  </head>

  <body>
    <div class="page-container flex-column justify-space-around">
      <div id="content-wrap">
        <header class="display-flex justify-space-between align-center p-4">
          <h1>
            <a class="p-3" id="health-tracker" href="/">Health Tracker</a>
          </h1>
          <nav>

            {{#if logged_in}}
              <a href="/dashboard"><button
                  id="my-dashboard"
                  class="btn btn-light"
                >My Dashboard</button></a>
              <button class="btn btn-light" id="logout">Logout</button>
            {{else}}
              <a id="login-click" class="p-3" href="/login">Login</a>
            {{/if}}
          </nav>
        </header>
        <main class="container container-fluid mt-5">

          {{{body}}}
        </main>
      </div>
      <footer class="text-center" id="footer">
        Made with 💙 by Alviva, Levi and Tori!
      </footer>
    </div>

    {{#if logged_in}}
      <script src="/js/logout.js"></script>
    {{/if}}
  </body>

</html>