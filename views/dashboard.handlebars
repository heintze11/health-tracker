<div class="row">
  <div class="col-auto">
    <h2>Welcome, {{fname}} {{lname}}!</h2>
  </div>
</div>

<div class="row mt-4">
  <div class="col-md-6">
    <h3>Your Dashboard:</h3>
    <a>Doctors{{doctor}}</a>
    <a>Appointments{{appointment}}</a>
    <a>Lab Results{{labs}}</a>
    <a>Prescriptions{{prescriptions}}</a>

   
  </div>
</div>